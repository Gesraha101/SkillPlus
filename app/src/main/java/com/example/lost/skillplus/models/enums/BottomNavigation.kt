package com.example.lost.skillplus.models.enums

enum class BottomNavigation(val key: String) {
    CATEGORY("category_content_fragment"),
    FAVOURITES("favourites_fragment"),
    NOTIFICATIONS("notifications_fragment")
}